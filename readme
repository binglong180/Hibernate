====================================================================================
投影查询
====================================================================================
1、就是在HQL语句中加入select 语句，如果只是展示数据建议使用投影查询
单参数
public List<String> getEmpJob() {
		return this.currentSession().createQuery("select JOB from Emp").list();
	}
多参数获得的是数组
public List<Object[]> getEmpJob_2() {
		return this.currentSession().createQuery("select ENAME,JOB from Emp").list();
	}
还可以使用构造
public List<Emp> getEmpJob_3() {
		return this.currentSession().createQuery("select new Emp(ENAME,JOB) from Emp").list();
	}
====================================================================================
分页查询
====================================================================================
1、uniqueResult() 获得总页数
2、setFirstResult((currentPage - 1) * pageSize) 开始记录
3、setMaxResults(pageSize) 页面大小
====================================================================================
按条件Map询绑定各种参数setPorperties
====================================================================================

====================================================================================
按条件(参数类)查询绑定各种参数setPorperties
====================================================================================

====================================================================================
按条件查询绑定各种参数setPorperties
====================================================================================


====================================================================================
按条件查询
====================================================================================

====================================================================================
使用iterate() 查询集合
====================================================================================

====================================================================================
使用List() 查询集合
====================================================================================



====================================================================================
使用merge() 更新数据
====================================================================================

====================================================================================
使用Hibernate实现数据的修改
====================================================================================
**修改前必须先对数据进行加载
1、修改方法为load()/get();

2、删除的是对象

**删除与修改前必须加载
====================================================================================
使用Hibernate实现主键查询
====================================================================================
1、使用get() 进行加载
2、使用load() 进行加载
二者有什么不同？

====================================================================================
简单的实现了hibernate的持久化数据
====================================================================================
问题1 ：
hibernate.cfg.xml not found 在Myeclipse进行设置

问题2 ：
org.hibernate.HibernateException: Unable to get the default Bean Validation factory
validation 没有定义

在配置文件中加入
<property name="javax.persistence.validation.mode">none</property>
====================================================================================